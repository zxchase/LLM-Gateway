[project]
name = "llm-gateway"
version = "0.1.0"
description = "LLM Gateway service split from Agent.py"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "httpx>=0.27",
    "jsonschema>=4.18",
    "pydantic>=2.6",
    "uvicorn>=0.30",
]

